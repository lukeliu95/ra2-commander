# ra2-commander

**让 Claude 的多个 AI Agent 实时指挥网页版红色警戒2，在 [gonghui.k0s.cn](https://gonghui.k0s.cn/)（《王二火大 / Chrono Divide》）上和电脑 AI 打遭遇战，并且每打一局就复盘、学习对手、进化出下一代打法。**

> A [Claude Code](https://claude.com/claude-code) skill that lets multiple AI agents command a Red Alert 2 web remake (Chrono Divide / 王二火大 at gonghui.k0s.cn) in real time against the built-in AI, then review each match, learn the opponent's tactics, and evolve the next generation of strategy.

---

## 它能做什么

- **接管游戏**：在页面里注入一个 250ms 的执行层，自动完成展开基地车、建造、生产、放置建筑、部队集结、迎击、修理、进攻和拆家。
- **多 Agent 实时指挥**（至少 2 个 subagent 同时在线）：
  - **指挥官**：读战场情报，调整姿态（守/攻/骚扰）、兵种比例、防御、出击时机。
  - **情报分析员**：盯对手的建造、兵力、行军路线，对照对手档案，提前发预警。
  - **后勤官**（可选第三个）：负责经济和生产节奏。
- **赛后学习**：复盘学习员导出全局数据，生成双方时间线，更新**对手档案**和**战术库**（每条战术带证据和置信度）。
- **进化**：基于证据生成下一代方案（带版本谱系和战绩）。下一局自动使用最新方案；新方案连续输棋时回退。

## 实战记录

| 对局 | 打法 | 结果 |
|---|---|---|
| match-001 | 固定脚本，没有 agent | 3:45 被 AI 的一波 10 单位主攻推平 |
| match-002 | 执行层 + 实时指挥官 subagent | **7:27 胜利**，零建筑损失，拆光 AI 全部建筑 |
| match-003 | 指挥官 + 情报分析员同时在线（方案 v002，skill 首次完整测试） | 7:34 失败。AI 换成苏军，出生点也互换了；它每 1.5–2 分钟进攻一波，一波比一波强（动员兵 → 犀牛 → V3 → 天启）。分析员识破了阵营和出生点变化，10 条预警的兵力判断都准，但到达时间估晚了 |
| match-004 | 方案 v003，专门验证 match-003 的执行层修复（双方镜像苏军局） | 7:56 失败，但败因彻底变了：拴绳、队列对账、V3 弹体过滤、前锋 ETA 四项修复全部实战验证有效——14 次警报迎击点全部拉回塔旁，0 次全军被拖出碉堡（上局 2 次全灭），`enemy.lead` 的 ETA 6/6 全对（上局 6 次只 2 次准）。真正输因变成资金从 1:20 起恒为 0（四条生产队列同时分钱，1500 的磁暴线圈 3 分半没造完）和天启提前 1:35 出现 |
| match-005 | 方案 v004（矿车/矿厂加量、磁暴线圈写进初始方案） | 3:49 失败，本系列最快的一局。对手首次是第三阵营 Confederation（古巴）：坦克 2:32 就到、波次间隔仅 29 秒。真正的技术问题是迎击点只按固定拴绳半径算，不看防御塔的实际武器射程——部队被派到塔火力覆盖不到的地方对射送空；另外发现执行层完全没有古巴的建筑名映射表，纯粹是运气好没有我方抽到这个阵营 |
| match-006 | 方案 v005（迎击点按塔射程截断、补古巴阵营表） | 9:25 失败，本系列最长也是最惨的一局。对手第一次是盟军镜像局，打法和前几局的"连续压制"完全不同：闷头攒经济、囤到 17 单位/12630（我方 2.5 倍）才一次性总攻，30–40 秒内拆穿基地。指挥官和分析员其实提前看到了囤兵信号，但把它当成"经济要不要跟上"的慢思考问题处理，等反应过来时总攻已经到了家门口。对局中用户实时指出三个问题：卧倒步兵的"射程增加"其实是"伤害 +67%"（现场用 `game.rules` 核实）；迎击点质心抖动会打断正在进行的攻击（已修复，下一局验证）；囤兵信号该更早被抓到、该主动打经济。赛后学习员接手时页面已经回到主菜单，导出流程数据丢失了一大半，已经修了这个漏洞（`over` 判定后自动把数据写进 localStorage 兜底） |
| match-007 | 方案 v006（防空写进初始方案、囤兵自动预警、矿车护卫） | 5:22 失败，本系列第二快。对手又是盟军，但换了第三种打法——早期步兵伏击 + 后期 JUMPJET 空降斩首：3:16-3:41 一场仗里 3 辆灰熊 + 5 个步兵被 5 个卧倒的敌方步兵磨到全灭（数量打平但输了坦克），紧接着 7 架 JUMPJET 直接飞进基地内部停驻超过一分钟，我方零防空、零部队，40 秒内被拆穿。用户看完战报直接追问"依然给坐下的步兵摧毁大部分"，逼出了这局复盘里最有价值的一条代码审计：match-006 加的"静止步兵伤害加权"机制**算出来了但从没接入任何撤退判断**——防守分支此前无条件死磕到全灭，没有任何战损比退出条件；另外现场核实到一个新机制——卧倒步兵当被攻击目标时还有额外的减伤加成，真实劣势约是表面数字的 3.3 倍。用户还追问了一个更深的问题："是 LLM 反应太慢还是策略有问题"——回顾发现三类问题分层清晰：执行层里的自动规则（比如"10 秒没见敌人就出击"）会在 LLM 反应过来之前就把资源花出去；LLM 的战略判断经常是对的但落地要 1-2 分钟，这个时间差是架构性的；v005 把防空车权重整个拿掉是对错误对手的过拟合，现在被两局盟军对手证明是误判 |

### 四次进化：match-004～match-007 输了之后发生了什么

**match-004（验证局，v003 → v004）**：复盘确认 match-003 那批执行层修复全部生效（细节见上表），把真正的败因——四条生产队列同时抢资金、贵建筑永远排最后——修成了"资金不足且有 ≥1000 的建筑在建时暂停新排步兵/载具"的饥饿保护；顺手发现并修了 `visibleArmy` 混入敌方"已探索但当前不可见"区域单位的问题（这个引擎没有战争迷雾，探过的地方永久可见），把情报拆成 `field`（野战部队）和 `atEnemyHome`（免费的科技侦察）两块。方案层 v004 只改经济：`minersPerRefinery` 2→3、`maxMiners` 7→9、防御加 `strongDef:1`。

**match-005（v004 首战，v004 → v005）**：对手是全新阵营，五局里第一次遇到，兵力池（PLA/GHOST2/LTNK）不在档案里；用赛后规则数据反查出 GHOST2 是一发 C4 拆楼的海豹步兵、LTNK 装甲和灰熊打对台但攻击力更高。复盘发现新饥饿保护的"矿车豁免"让第 5 辆矿车占了关键窗口的生产队列，以及迎击点算法的老毛病——只看拒绳半径不看塔的真实射程（碉堡 5.5 格、光棱塔 8 格，之前迎击点经常派到 8–10 格外）。方案层 v005 改 3 个字段：`minersPerRefinery` 回 2、`vehicleMix` 全押坦克、`leashRadius` 8→4；执行层修了迎击点按塔射程截断、补了古巴阵营的建筑名表、修正了一个会把古巴重坦当成炮兵触发自杀式出击的正则、给矿车豁免加了数量上限。

**match-006（v005 首战，v005 → v006）**：对手第一次是盟军镜像局，玩法从"连续压制"变成"闷头攒经济、一波流总攻"，指挥官看到了囤兵信号但选错了应对的时间尺度（拿见效慢的经济调整去应对迫在眉睫的总攻）。用户实时旁观并给了三条关键反馈：① 现场用 `game.rules.getObject/getWeapon` 核实，卧倒步兵是"伤害 +67%"（E1 的 M60→M60E）不是常被误认的"射程变化"，`threatValue` 按 cost 算会严重低估静止步兵的真实输出；② 迎击点质心每 0.8 秒都按威胁群实时位置重算，质心一漂移就会重发 `AttackMove` 打断正在进行的攻击，看起来像"部队站着不还手"；③ 囤兵信号应该更早被系统性抓到、经济健康时应该主动打对方经济，不能一直被动防守。这局还额外发现一个流程漏洞：学习员接手时页面已经回到主菜单，`window.__cmd` 没了，赛后归档的原始数据丢了一大半。执行层这次改了六处：迎击点取整到 2 格网格减少指令抖动、静止步兵按 1.6 倍伤害权重计入威胁值、囤兵比例超阈值自动发预警（不用等人工发现）、可选的矿车常驻护卫（默认关闭）、`over` 判定后立刻把归档数据写进 localStorage 兜底、`learner.md` 加了"数据没了就如实报告，别当成自己操作错误"的强提醒。三局里出现过的兵种数据也系统性整理进了新的 `knowledge/units.json`（含武器伤害/射程/装甲克制表），供以后不用每局现场重查。方案层 v006 改 3 个字段：`targetRefineries` 3→4、`maxMiners` 9→12（把指挥官中局才想到的经济加码提前到开局）、`attackMinUnits` 14→10（呼应用户"主动打经济"的要求，降低指挥官主动出击的门槛）。

**match-007（v006 首战，v006 → v007）**：又是盟军对手，但玩法是第三种——早期步兵伏击 + 后期 JUMPJET 空降长驻拆家。用户看完战报直接追问"依然给坐下的步兵摧毁大部分"，逼出了这局最关键的一次代码审计：match-006 加的静止步兵伤害加权（`threatValue` 按 1.6 倍算）只喂给了日志展示和"进攻部队要不要回防"两处，**防守分支本身从头到尾没有任何撤退判断**，3 辆灰熊 + 5 个步兵原地被磨到 0 也不会主动撤——这不是权重数值选小了，是压根没接上决策链路。现场用 `game.rules.getWarhead().proneDamage` 还核实到一个新机制：卧倒步兵当被攻击目标时会额外减伤（AP 武器只有平时一半的伤害），综合下来真实劣势约是表面数字的 3.3 倍。用户还追问了一个更根本的问题——"整个指挥战斗能力不行，是 LLM 不够快还是方案有问题"，逼着做了一次系统性复盘：执行层里有几条自动规则（比如"10 秒没看到敌人、单位数够了就出击"）会在任何 LLM 反应之前就把资源花出去，这是设计问题不是速度问题；LLM 的战略判断记录下来几乎全是对的，但从"看到问题"到"改完方案生效"往往要 1-2 分钟，这个窗口期的损失只能靠给执行层加更多自动预警去缓解，不能靠让 LLM 想得更快解决；v005 把 `vehicleMix.aaVehicle` 整个拿掉是对"古巴/苏军通常没有空军"的过拟合，现在两局盟军对手都带 JUMPJET，这个决定被验证是错的。执行层这次改了五处：给防守交战补上退出条件（`defendRetreatRatio`，跌破比例就撤回基地不再硬拼）、敌机实际逗留基地附近时防空建筑绕开资金门槛、攻城自动出击要求至少含 1 个载具（避免纯步兵冲锋）、持续交战每 10 秒刷新一次日志避免低估交战规模。方案层 v007 改 3 个字段：`defenses.aaDef` 缺省→1（防空写进初始方案，不用等触发）、`siegeAutoAttackUnits` 8→14（吸收本局指挥官临场验证过的修正）、`vehicleMix.aaVehicle` 恢复为 1。

### 一次完整的进化：match-003 输了之后发生了什么
1. **复盘学习员**导出全局数据，生成双方时间线，判断主要输在执行层，其次是方案按盟军设计、对苏军准备不足：
   - 迎击反射两次把全军拉出碉堡，部队价值 2790→180、2430→0；
   - 改方案取消不了已经在建的项目，5 次止血操作全部无效。
2. **对手档案**为苏军新开小节：4 波进攻的出门时间和兵种构成、行军路线、"没有反攻窗口"、专打落单矿车。
3. **战术库**新增 T-011～T-015：只在防御塔旁接战、对苏军以坦克为主、第 2 座工厂不写进建造顺序等；T-001、T-007 升为"确认"。
4. **方案 v003**（父版本 v002，只改 2 个字段）：建造顺序删掉第 2 座战车工厂，防空车权重 2→1。
5. **执行层修复**（主会话审核学习员的问题清单后修改，并做了冒烟测试）：
   - 拴绳防守 `leashRadius`；
   - 改方案会自动取消在建项并退款；
   - V3 导弹不再被当成空军；
   - 情报新增敌军前锋位置和到达时间 `enemy.lead`；
   - 国家专属建筑名自动匹配。

这些经验已经作为初始知识写进 `assets/seed/`：方案 v000→v007、对手档案 `ai-easy`（盟军三种打法、苏军、古巴五个小节）、战术库 T-001～T-028、七局的完整记录，外加一份跨局的单位数据库 `knowledge/units.json`。下一局默认使用 v007。

## 架构

```
┌────────────────────────── 浏览器页面（gonghui.k0s.cn）──────────────────────────┐
│  游戏引擎 ──Object.prototype 陷阱──▶ GameApi / 命令接口                            │
│                                         │                                          │
│  runtime.js 执行层（每 250ms）：生产 · 放置 · 部队 · 反射 · 攻城 · 记录器          │
│      ▲ plan（按角色分配写权限）          │ intel（公平视野，每个读者独立游标）     │
└──────┼───────────────────────────────────┼─────────────────────────────────────────┘
       │ C.apply / C.advise                ▼
  ┌──────────┐   ┌──────────────┐   ┌────────────┐
  │ 指挥官   │◀──│ 情报分析员    │   │ 后勤官(可选)│   ← 实时 subagent（约 10–15 秒一轮）
  └──────────┘   └──────────────┘   └────────────┘
       │ 对局结束
       ▼
  ┌──────────────┐  归档 → 时间线 → 对手档案 / 战术库 → 下一代方案 → 执行层问题清单
  │ 复盘学习员    │─────────────────────────────────────────────────────────────────▶ 下一局
  └──────────────┘
```

设计要点：
- **反应和决策分层**：LLM 思考一轮要 10–30 秒，战斗却是秒级的。秒级反应（迎击、补电、修理、防空、撤退、集火建筑）由执行层完成；agent 只负责战略参数。
- **公平视野**：实时 agent 只能读我方可见的情报。全视野的记录器数据只在赛后复盘时使用。
- **进化数据和代码分离**：战绩、方案版本、对手档案、战术库保存在 `~/.claude/ra2-commander/`，升级 skill 不会丢失。

## 环境要求

- [Claude Code](https://claude.com/claude-code) **桌面版**（Code 标签页），需要内置浏览器面板（`mcp__Claude_Browser__*` 工具）来打开并注入游戏页面。
- Python 3.8+（运行 `scripts/*.py`）
- Node.js（可选，只用于 `node --check` 做语法检查）
- 已测试环境：gonghui.k0s.cn 客户端 v0.87.0，单机模式 → 遭遇战，AI-简单，地图"岛屿之战 (2)"。我方打过盟军（法国）和苏军；对手遇到过盟军、苏军、Confederation（古巴，第三阵营）三种，出生点会互换，双方也可能是同阵营镜像局（match-006、match-007 都是盟军 vs 盟军，但打法完全不同）。

## 安装

把仓库克隆到 Claude Code 的用户级 skills 目录：

```bash
git clone https://github.com/lukeliu95/ra2-commander.git ~/.claude/skills/ra2-commander
```

重新打开 Claude Code 会话后，skill 列表里会出现 `ra2-commander`。

## 使用

在 Claude Code 桌面版里直接说：

```
打开 https://gonghui.k0s.cn/ ，用 ra2-commander 打一局遭遇战
```

或者更具体一些：

```
用 ra2-commander 打一局，开指挥官、情报分析员和后勤官三个 agent，打完复盘并进化方案
```

Claude 会按 `SKILL.md` 的流程执行：

1. `evolve.py init / status`：初始化数据目录，查看当前方案版本和战绩。
2. 在内置浏览器打开游戏，停在主菜单。
3. `evolve.py new-match`：生成本局目录、注入代码（install.js / arm.js）和各角色任务书，在**开局前**注入执行层。
4. 并行派出实时 agent（默认指挥官 + 情报分析员）。
5. 点击 单机模式 → 遭遇战 → 开始游戏。执行层第一秒接管，agent 实时指挥。
6. 对局结束后，派出复盘学习员：归档、更新对手档案和战术库、晋升新方案版本、列出执行层问题。
7. 向你汇报胜负、关键决策、对手的新打法和进化内容。

中途可以随时插话，例如"没有敌军就直接拆主基地和电厂"。Claude 会把指令转成方案修改，或者给执行层打补丁。

### 常用命令

```bash
S=~/.claude/skills/ra2-commander/scripts
python3 $S/evolve.py status                                   # 方案版本谱系和战绩
python3 $S/evolve.py new-match --opponent ai-easy --map "岛屿之战 (2)" --roles commander,analyst,quartermaster
python3 $S/evolve.py record --match match-004 --result won --duration 430
python3 $S/evolve.py promote --candidate candidate.json --reason "T-005: 更早出击"
python3 $S/evolve.py use v002                                 # 回退到某个方案版本
python3 $S/summarize_match.py ~/.claude/ra2-commander/matches/match-004
```

数据目录可以用环境变量 `RA2_DATA` 指定。想从头开始，删掉 `~/.claude/ra2-commander/` 再运行 `init`，会重新从种子数据初始化。

## 仓库结构

```
SKILL.md                    skill 主流程（Claude 读这个）
scripts/runtime.js          页面内执行层：接管、生产、部队、反射、攻城、情报、权限、记录器
scripts/evolve.py           数据目录、开新局、生成注入代码和任务书、登记战绩、方案晋升与回退
scripts/summarize_match.py  赛后数据 → 双方时间线
agents/commander.md         实时：指挥官
agents/analyst.md           实时：情报分析员
agents/quartermaster.md     实时（可选）：后勤官
agents/learner.md           赛后：复盘学习员
references/plan-schema.md   方案字段、反射、角色权限、情报接口
references/game-api.md      引擎接口、枚举、单位名、坑点清单、游戏更新后如何重新逆向
references/ui-navigation.md 菜单操作
assets/seed/                初始经验：方案 v000–v007、对手档案、战术库、七局记录（含每局全部复盘产物）
assets/seed/knowledge/      跨局累积的单位数据库（武器伤害/射程/装甲克制表），随 evolve.py init 一起初始化
```

## 扩展

- **加角色**：在 `agents/` 下写任务书模板，在 `runtime.js` 的 `C.ownership` 里给它分配方案字段；只提建议的角色用 `C.advise`，不需要改代码。可以参考的方向：侦察官、空军官、微操官。
- **打更强的 AI**：`new-match --opponent ai-medium`，会建立独立的对手档案。
- **游戏更新后接口失效**：按 `references/game-api.md` 第 8 节重新定位接口。

## 使用规则

- **只用于单机模式对战电脑 AI。** 不要在排位赛或联机大厅里使用：对真人使用自动化就是作弊，skill 本身也会拒绝。
- 请尊重游戏站点和社区，不要高频刷局，也不要做任何影响服务器的操作。
- 本项目通过读取客户端内存对象、调用游戏自带 AI 使用的同一套命令接口来操作，不修改服务器，也不包含或分发任何游戏代码和素材。

## 免责声明

本项目是独立的非官方实验项目，与 Electronic Arts（EA）、《命令与征服》/《红色警戒》系列版权方、"王二火大 / Chrono Divide"项目及 gonghui.k0s.cn 站点均无任何关联。相关名称和商标归各自所有者所有。游戏客户端更新可能导致本项目失效。

## License

[MIT](LICENSE)
