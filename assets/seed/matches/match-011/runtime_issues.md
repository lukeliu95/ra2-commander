# 执行层问题 match-011

runtime = `rt-96acec8dee`，plan = v009（第 2 局），结果**胜**（10:21）。`window.__cmd` 复盘时仍存活，`meta.json`/`log.json`/`snapshots.json` 直接从 `dump()` 导出，`logCount 301`、`snapshotCount 63` 与 `meta` 自述一致；`C.mem`（运行时状态 `M`）也还在，下面的归因用到了 `M.lastTick`/`M.lastOrder`/`M.siegeHit` 和 10 秒粒度的原始快照。

本局没有 `error` 级日志；15 条 `warn` 全部是拴绳/资金/重型单位逗留/建筑队列空闲提示，属预期行为。**上一局修的两条（矿车复工、`defendMinValue` 前提）本局都按预期工作**：6 次"矿车在防线外被打：撤回基地"（3:50/4:04/4:18/4:36 …）之后 4:31/4:38/4:59 三条"重新派回矿区"，HARV 数一路 3→5→6→8、0 矿车损失；`defendMinValue` 只在 1:34（88%/630，确实损了 1 E2）和 3:22（39%/990）触发，1:18/1:41/2:10 零战损的交战不再误报。

---

## 1.（关键，已修复待验证）攻城被 1 辆小股敌军卡住近 3 分钟：`siege()` 的"附近有敌军就暂停拆楼"不配任何主动攻击，`army()` 的 AttackMove 又已到点，16 个单位在敌基地里空转

- **现象（log.json + 10 秒快照）**：5:40 攻城分队 14 单位/5320 出发，6:06 抵达敌家北侧，6:06–6:09 依次选中 GAREFN/GAPILL/GAPOWR/GACNST 作为拆楼目标（是建筑被陆续侦得的顺序，不是目标抖动），**6:09 `附近出现敌军，暂停拆建筑，先打部队` 之后到 9:00 整整 2 分 51 秒没有一条 `siege` 事件**。期间：
  - 6:12–6:33 击落 4 架单架补产的 JUMPJET，6:21/6:27/6:30 拆掉 2 GAPILL+ATESLA——这三座是会开火的防御建筑，被 AttackMove 到点后处于警戒态的单位还击拆掉的，不是 siege 的功劳（T-006：AttackMove 不拆普通建筑，本局再次印证）。
  - 6:33–8:37 **零建筑击破**。敌方常驻家里的只有 1 辆 56% 血、停在 GADEPT (142,84) 旁 (141,86) 的 FV（快照 t=402–532，8:22 到 8:52 坐标一格没动）+ 每 20–40 秒补 1 架的 JUMPJET + 补出来的 CMIN；分队质心 (128–133,80–84)，离 FV 9–10 格——落在 `siege()` 第 612 行 `near` 的 12 格判定内、又在 HTNK 射程（5.75）外。
  - 我方在这段空转里被碉堡/FV 点掉 **4 HTK + 6 E2（≈2600）**（7:20/7:23/7:56/8:40 HTK，7:21/7:24/7:26×2/8:43/8:56 E2），敌方趁机把 GAPILL 从 3 座补到 5 座（7:26 @130,89、8:02 @134,90），并补出 1 FV（7:26 atEnemyHome 1→3，7:27 被杀 1）。
- **代码原因（rt-96acec8dee）**：`siege()` 里 `near.length` 为真时只做两件事——清空 `M.siegeTarget`、写一条日志——然后 `return`，**没有对挡路单位下任何指令**；同时 `army()` 的进攻分支（`runtime.js:531-532`）只给"空闲"单位每 4 秒重发一次 `AttackMove` 到 `A.target`，而分队早已到点，AttackMove 立即完成、单位又空闲，如此循环，AttackMove 的自动交战只对进入武器射程的目标生效，9 格外的 FV 永远碰不到。两段逻辑互相假设对方会处理"附近那个敌人"，结果谁都不打。
- **影响：本局最大的用时损失**（≈3 分钟，外加 ≈2600 战损和敌方多补的 2 座 GAPILL），不影响胜负——分队价值 8000+ 对敌方 600，拖多久都不会输，但换成 match-010 那种它家里还能攒出 MGTK/SREF 的局面，这 3 分钟就是翻盘窗口。
- **修复（主会话已做，rt-579f6a23dc，`node --check` 通过，下一局 new-match 自动重装）**：`siege()` 在 `near.length` 分支里比较挡路敌军总价值 `nv` 与分队总价值 `gv`，`nv <= gv*0.25` 时全组 `Attack` 离质心最近的那个敌单位（6 秒去重），日志 `[热补丁] 拆建筑被 … 挡住：全组直接攻击 …`。本局 9:55/10:06 两次热加载版触发，各在 1 秒内继续拆楼（9:56 GAWEAP 倒、10:06 GAPILE 开拆），**标为已修复（待下一局验证）**。
- **归因裁定（主会话要求：指挥官 8:35 `attackTarget=[135,80]`、主会话 8:43 热补丁、9:00 攻城恢复，三者谁的功劳）**——结论：**都不是，停滞是被那辆 FV 自己死掉解除的；分析员"显式目标直接解除停滞"的归因不成立，指挥官"无法归因"是对的，且可以进一步收窄**。证据逐条：
  1. `near` 判定（`siege()` 612 行）只看分队单位与敌方非建筑单位的几何距离，**与 `attackTarget`/`M.attack.target` 无关**——改目标不可能直接清空 `near`，只有 FV 死掉或被拉开 >12 格才行。
  2. 8:35 apply 后 `M.attack.target` 立即改成 (135,80)（716 行），但 `M.lastOrder` 里键为 `atk:135,80` 的只有 3 个单位（id 3267/3298/3647，t=8:35/8:39/8:51）——都是刚从家里到达集结点的**增援**，原分队单位因为 AttackMove 早已完成、每 4 秒被重发的也是同一目标，没有任何位置变化：分队质心 8:32 (133,80) → 8:42 (133,80) → 8:52 (133,79)（快照 t=512/522/532，**8:32 就已经在 (133,80)，比 apply 早 3 秒，指挥官日志"钉坐标后分队移到 (133,79)"是把先后顺序看反了**）。8:37 那座 GAPILL 倒下是 apply 后 2 秒，一座 4 格外、早已在互射的碉堡，不可能是移动指令的结果。
  3. 热补丁：`every('siegeClear', 10)` 首次调用必返回 `true`（`runtime.js:88`，`M.lastTick[key] ?? -1e9`），所以补丁只要下过一次指令就一定会立刻留下 `[热补丁]` 日志；**第一条 `[热补丁]` 在 9:55**，8:43–9:55 之间一条都没有 → 补丁在 9:00 之前没有下过任何指令，与主会话自己"9:55 起实际触发"的说法一致。9:00 的恢复与补丁无关。（为什么 8:43 加载后 FV 还在 12 格内、价值 600 ≪ 25%×8330 却没触发，主会话需要核实热加载是否真的替换了主循环里持有的 `siege` 引用，还是 9:55 前又重装过一次。）
  4. 9:00 `没有敌军，拆建筑：GACNST` 出现在 9:01 `击杀敌方 FV` **之前一秒**——`near` 清空是因为 FV 已经死了（击杀检测滞后一个 tick），不是分队离开了它：分队质心反而从 (133,79) 向西移到 (128,83)（t=542），离 FV 更远。
  5. 谁打死了 FV 日志里没有直接证据；它 56% 血在维修厂旁停了 2 分半，最可能是 AI 让它动了一下（或维修完毕出动）进了警戒态单位的射程。apply 后 25 秒、补丁加载后 17 秒发生，两者都构不成因果。
  **结论写法**：攻城恢复 = FV 死亡；`attackTarget` 的作用只有"把 3 个新增援直接派到 (135,80)"；补丁的验证样本是 9:55/10:06 两次，各在 1 秒内恢复拆楼——这两次才是它有效的证据。

## 2.（主要，未修复）攻城阶段的增援逐个单兵深入，穿过敌方碉堡群送死；`attack.at`/`attack.count` 因此是全军而不是攻城分队

- **现象**：转攻后新出厂的单位在 `army()` 进攻分支（`runtime.js:523-532`）里先 AttackMove 到家门口 `rally`，到 10 格内就被加进 `A.ids`，下一个 4 秒周期"空闲"就单独 AttackMove 到 `A.target`——每个单位独自走 70 格中路，从西南进敌家时正好穿过 CNST 南侧的 GAPILL 群 (130–135,84–90)。7:20–7:26 七秒内损 2 HTK+4 E2，整局 24 E2+7 HTK 损失大半是这样送掉的；8:01 指挥官 `infantryCap: 0` 后 E2 损失才停。
- **副作用**：因为所有单位一出厂就进 `A.ids`，`status.attack.count === me.army.count`、`status.attack.at` 是全军质心（767 行 `strikeGroup` 实际就是全军）——指挥官 6:46 读到 (108,94)、7:27 读到 (123,86)，实际分队一直贴在 (128–134,80–84)，analyst 7:49 据此误判"分队退到南侧碉堡群"。
- **建议改法**：(a) `army()` 进攻分支里增援不要单独出发：在 `rally` 攒到 ≥4 个单位或 ≥1500 价值（或 20 秒超时）再整批 AttackMove 到 `A.target`；步兵在 `stance === 'attack'` 且敌家已侦得防御建筑时留在 `rally`（只作防守）。(b) `A.ids` 的加入条件改成"离 `A.target` 或分队质心 20 格内"，`status.attack.at/count` 只统计这些单位，让实时角色看到的是分队本身。(c) 方案层：T-034（指挥官在转攻同轮 `infantryCap: 0`，盟军局）作为过渡。

## 3.（主要，未修复）`targetRefineries` 对账排产没有资金门槛；对账取消不看进度

- **现象**：第 4 座 NAREFN（2000）由 `production()` 242 行 `counts < P.targetRefineries` 补建，**不看资金**：4:10 资金 101 时排产（首波刚打完、部队 990、第二波 4:29 就到），立刻触发"资金不足且有 ≥1000 的建筑在建：暂停新排步兵/载具"；指挥官 4:41 把 `targetRefineries` 降到 3，4:42 对账取消时**进度已 75%**（退款 1500，但 32 秒建筑队列白扔——早知道 75% 就该让它造完）。7:21 恢复到 4 后又在资金 640 时排产，7:27/8:10/8:18 三次资金饥饿反射正好卡在攻城最需要 HTNK 的时候，第 4 座 8:51 才落地，对结果零贡献。match-010 同一字段从未达成，match-004 5:14 同类事故（T-013）。
- **建议改法**：(a) 242 行补建矿厂/第 2 工厂这类 ≥1500 的非 buildOrder 项加门槛：`credits >= cost*0.5 && !M.alarm`（`buildOrder` 里的项按 T-013 由方案层控制，不动）；(b) `reconcileQueues()` 对 Structures 队列里进度 ≥50% 的项不取消（只取消排队中/低进度的），或至少把进度写进决策：日志已经打印进度，只差一个判断。v010 已把 `targetRefineries` 改为 3 作为方案层的过渡。

## 4.（次要）TESLA#1 3:27 才落地（match-010 是 2:26）——资金窗口被 NAFLAK#2 和第 3 矿厂占掉，且 3:20 低电 -110

- 2:24 雷达落地即可造 TESLA，但 2:19 NAFLAK#2（1000）、2:41 前第 3 矿厂（2000）、HTNK 三方分零收入，Armory 队列 `credits > 700` 门槛到 2:53 才过（资金 1103），3:20 电力 -110 又拖慢建造，3:27 借"敌方重型单位逗留"反射绕过门槛才落地——首波 3:03–3:29 它 93% 没参战。不是 bug，是 v009 在 2:20–2:50 这个窗口的资金竞争；v010 把 `aaDef` 预置到 2 会让 NAFLAK#2 在雷达前的 1:01–2:20 排产，把这个窗口让给 TESLA（待验证）。

## 5.（次要）指挥循环延迟 35–50 秒（不是执行层，但影响执行层字段的可用性）

- 6 次 apply 的落地时间都比指挥官读情报晚 35–50 秒（1:32→2:14、3:22→4:08、4:22→4:41、6:46→7:21、7:36→8:01、8:16→8:35）。"取消在建项"这类时效动作因此不可靠（#3 的 75% 取消）；6:15 一轮还 `droppedEvents: 11`（5:26–5:54 的事件被 `maxEvents` 截掉）。建议：`intel()` 的事件缓冲优先丢 `kill`/`loss` 之外先丢 `reflex` 类，或把 `maxEvents` 提到 60；指挥官侧则把"取消"类决策限定在排产后 15 秒内。

## 6.（次要，已知）建筑队列空闲 warn（5:27/6:42/9:36）

- `buildOrder` 9 项 5:27 前全部完成，此后建筑队列只在补电厂/矿厂时工作。无兜底项是设计如此；本局电力全程 190–265 余量，不需要追加。可以考虑在 `stance === 'attack'` 且资金 >2500 时自动补一座电厂/防御之外什么都不做——目前没有证据需要改。

## 7.（观察，不是问题）自动转攻反射第一次正确自触发

- 5:39 `攻城规则：10 秒敌方无野战部队，我方 14 个单位（含载具、价值 5320）对敌方 0（T-005 优势达标）`——match-009 修的 `field` 判据 + `hasVehicle` + 2 倍门槛本局全部按设计工作，时机与 T-024/T-031 一致；`siegeAutoAttackUnits: 14` 恰好避开了 5:10 第三波进场（12 会在 5:12 把 13 单位送出门）。
