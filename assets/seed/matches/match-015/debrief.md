# match-015 复盘总结（debrief）

> 撰写者：复盘学习员（learner）｜对局：match-015｜对手：`ai-easy`（盟军 GA）｜地图：岛屿之战 (2)
> 我方：**苏军 NA（Player 1）@135,80（东北）**｜敌方：**盟军 GA @71,118（西南）**——**match-014 的完整镜像**
> 方案：**v013（首战）**｜runtime：**rt-5e310d3147**｜结果：**胜，6:59**（`meta.durationSec = 420`；页面 `[over]` 文案为 6:59，`timeline.md` 表头四舍五入写 7:00）

---

## 0. 归档与数据完整性

| 产物 | 状态 |
|---|---|
| `meta.json` | `window.__cmd.dump('meta')` 原样导出（logCount 224 / snapshotCount 43 / plan0 = planFinal = v013） |
| `log.json` | 182 条（排除 42 条 build/prod），seq 覆盖 1–224 无缺号 |
| `snapshots.json` | **43 张全量（`stride:1`）**——任务书模板写的是 `stride:2`（每 20 秒一张），但本局关键交战在 5:13–5:24，20 秒粒度会漏掉它；先按模板取了 22 张后补取全量，两者都跑过 |
| `timeline.md` | `summarize_match.py` 生成，225 行 |
| `intel_notes.md` | **本局新增**：单位/建筑基础造价实测表 + 全部窗口的逐条重算结果（供后续局对照；本文件是下节口径警告的支撑材料） |
| 战绩登记 | `evolve.py record --match match-015 --result won --duration 420`；`status` 确认 v013 → 1 胜 0 负，`recentMatches` 已含 match-015 |

**两处数据口径校正（本 debrief 与战术库一律按校正后的口径引用）**
1. **"38:1" 的分子分母取自两个不同时刻（口径不匹配），正确值是 35:1**：`[loss]` 事件只有 3 条发生在 6:26 之前——1:59 `{DOG:1}`（200）、2:50 `{E2:1}`（90）、2:53 `{E2:1}`（90），**到 5:24 的累计损失正好是 380**（分析员这笔没错）；问题在击杀侧——它把分子取到了 5:40 之后（含 5:42/5:45 附近的两只单兵，累计 14580），分母却停在 5:24 的 380。按 `log.json` 逐条重算：**到 5:24 累计击杀 13300 : 损失 380 = 35:1**（同刻取值才是正确的比值）。**必须同时明确：本局第二段（5:13–5:24）窗口本身确实零损**——该窗口内没有任何 `[loss]` 事件，**5:30–6:00 整段也是 0 损失**（先头部队 5:56 出发，第一条攻城段损失 6:26 才出现）。**引用交换比时一律以 `log.json` 逐条重算为准，不要引用实时 `tally` 的比值。**
   *附：实时角色另有 2 处小数级笔误，一并记为"实时 tally 不可作归档口径"的旁证*——6:39 那条写"6:25–6:32 损 HTK×2 + E2×3 = 1650"（逐条为 HTK 2 + E2 3 = **1270**，多算 380），6:58 那条写"累计损失 9320、全局 tally 34680:9320 = 3.72:1"（终局实际 **10310**，官方口径 **37220:10310 = 3.61:1**）。两处都不影响任何决策。
2. **`timeline.md` 的"拆建筑"是申请、不是战果**：`[siege] 没有敌军，拆建筑：X` 只表示"当前目标选定"，真正倒下要等 `[kill] 摧毁敌方建筑 X`。任务书给的收官要点与指挥官总结里写的"6:42 GACNST → 6:47 GAPILE → 6:48 GAAIRC → 6:57 GAWEAP"**与 `[kill]` 事件完全吻合**（实测 6:42 GACNST / 6:47 GAPILE / 6:48 GAAIRC+GATECH / 6:57 GAWEAP），**不等于** `siege` 行的顺序（siege 行是 5:57 GACNST / 6:42 GAAIRC / 6:47 GAWEAP / 6:57 GAPOWR）。

---

## 1. 结果与历史战绩对比

| 局 | 版本 | 结果 | 用时 | 备注 |
|---|---|---|---|---|
| match-010 | v009 | 负 | 6:56 | 复合速攻第 1 样本（首波被防住，第二段没防住） |
| match-011 | v009 | 胜 | 10:21 | 首次打赢盟军复合速攻 |
| match-012 | v010 | 胜 | 23:31 | 本系列最长；赢在清场、输在"来回" |
| match-013 | v011 | 负 | 8:01 | 古巴局，`retreatRatio 0.14` 回归 |
| match-014 | v012 | 负 | 9:06 | 双盟军镜像，第二段反装甲对位缺失 |
| **match-015** | **v013** | **胜** | **6:59** | **v013 首战；本系列对盟军复合速攻局里最快、最干净的一次（除 match-010 的 6:56 是负局外，唯一进 7 分钟的一场）** |

- v013 = v012 + 2 处改动（`strongDef 1→2`、`minerEscort 1→0`），**首战即胜**，两项改动本局都被检验到（见 §3）。
- 全局交换比 **37220 : 10310 = 3.61:1**（终局 `C.intel().tally`）。分项：击杀 ADOG 5 / E1 24 / FV 4 / MTNK 4 / JUMPJET 11 / CMIN 6（`log.json` 逐条累加**完全一致**）＋建筑 7 座；损失 DOG 1 / E2 9 / HTK 6 / HTNK 7。
- **单位造价口径（本局实测，务必照抄）**：从页面 `o.rules.cost` 实读——**E1 180 / JUMPJET 600 / MTNK 750 / FV 600 / ADOG 200 / CMIN 1400 / E2 90 / HTK 500 / HTNK 900 / HARV 1400**；建筑：NACNST 3000 / NAPOWR 600 / NAREFN 2000 / NAHAND 500 / NAWEAP 2000 / NALASR 500 / NAFLAK 1000 / NARADR 800 / TESLA 1500。**注意 `E2 = 90`、`HTK = 500`，不是实时角色日志里用的 180 / 600**——用错值逐条重算损失会得到 11720 而不是引擎的 10310（差 1410），**这也是 §0 口径警告的根源**。按实测值核算：击杀侧 54 个单位 = 25720，加 7 座建筑（引擎合计 11500，`GACNST` 在本引擎 `cost()` 基础值下与表值有别）= **37220，与引擎 tally 完全吻合**；损失侧 23 个单位逐条重算 = **10310，同样完全吻合**。
- 阵容与 match-014 完全镜像，但**胜负反转**：match-014 第二段（MGTK/SREF）我方 0.25:1、占全局损失 88.6%；本局它**根本没造出第二段**。

---

## 2. 胜负关键（3 条，带数据）

1. **两座反装甲塔预置在来向轴线上，把它两段攻势都吃在塔线前，我方 9 座防御建筑整局零损失。**
   `defenses {baseDef:3, aaDef:2, strongDef:2}` 从初始 plan 生效：NAFLAK#1 0:42 落地 @131,87、NAFLAK#2 0:52 落地 @130,82；**TESLA#1 2:10 排产（资金 1742）/2:46 落地 @130,81、TESLA#2 2:59 排产（资金 1113）/3:44 落地 @131,81**——两座都在 baseCenter 朝敌方向 6 格的轴线上（match-014 唯一那座被 `(k%3)-1` 轮转到正东、8:47 才第一次挨打）。结果：首波（2:46，11 单位/4530）2:57 全灭（我方损 2 E2 = 180）；第二段（5:03–5:07 实测 field 12 单位/7230 = 3 MTNK+6 JUMPJET+2 FV+1 E1，含 **6 架 JUMPJET**）**5:13–5:24 零损全歼**（该窗口 `[kill]` 合计 10230，含它顺带送进来的 2 辆 CMIN 2800）。我方全局仅损 1 DOG + 2 E2 = **380**，全部发生在 5:24 之前。终局 `buildings` 9 项齐全、`damaged` 为空。
2. **5:12 选择"让它先撞塔线"而不是抢在它出门前出门（T-031/T-043）。**
   5:12 它把 `atEnemyHome` 抽空到 1 单位/200、field 从 0 涨到 12 单位/7230；指挥官维持 `defend` 不出击。这一波被零损吃掉后，`atEnemyHome` 归零 ⇒ **它家变成一座空基地**，5:55 自动转攻反射触发、5:56 以 30 单位/17720 出发，**63 秒**（5:56→6:59）拆完判负口径的 4 类生产建筑。对照：分析员 4:19 与 4:54 两次建议转攻（当时 1.6:1 与 2.01:1），指挥官按"它正在补 3 座 GAPILL + 5:00 补 GATECH"逐条否决——4:19 那次出击就是 match-013 的同型剧本（那次 0.36:1 惨败）。
3. **经济把它压成了"资金受限"的一方，第二种雪球被掐死在摇篮里。**
   它整局 CMIN 峰值只有 **6 辆**（match-014 是 11 辆且 0 损失），被我方击杀 **6 辆（8400）**；第 3 矿厂拖到 **5:32**（连续 4 局 3:21/3:22 的定式被打破）、GATECH 拖到 **5:00**（档案 3:01–3:41，全档案最晚），**到 6:48 被我方拆掉为止一辆 MGTK/SREF 都没造出来**。这是全档案第一次观察到它的经济被彻底压住（T-044 新增）。

---

## 3. 对手新行为或变化（详见 `opponents/ai-easy.md` 的 match-015 时间节点表）

1. **"JUMPJET 首见即 ≥3–7 架混编首波"这条识别特征被推翻**：本局 2:43 首见**只有 1 架**，首波 2:46 是**纯地面 11 单位/4530（6 E1+2 FV+3 MTNK）**，那 2–3 架 JUMPJET 一直留在家，直到 5:03 才 6 架一次投入。识别"复合速攻"应改看"首波被全歼后它是否还在继续投入 + GATECH/矿车是否还在生产"，而不是首见规模。
2. **静默期可以长到 6 分钟**：2:57–5:03 约 6 分钟（match-010 是 90 秒、match-014 是 117 秒），期间 `atEnemyHome` 只涨 1 单位/1020（match-014 同期 `army.value` +8850）。**"短静默 = 换装（危险）"与"长静默且不涨 = 它经济被卡住（我方窗口）"是两种相反信号**，判据是斜率不是时长。
3. **军事节点前移、经济科技节点后移**：GAAIRC **1:07**（档案 1:41，早 34 秒，全档案最早）、FV 2:08（早 33 秒）、MTNK 2:22（早 19 秒）；但第 2 矿厂 2:03（晚 2–10 秒）、第 3 矿厂 5:32（晚 2:10）、GATECH 5:00（晚 1:19–1:59）。**"出生点镜像只改地理、不改秒表"这条只对骚扰点/来向成立**，对军事与经济节点都不成立。
4. **首次观察到"6 个 E1 成建制前推"**：1:12–1:36 共 6 个 E1 推到 (122,82)，被吃掉 2 个后撤回——此前盟军早期骚扰都是 1–2 单兵分批添油。
5. **静态防御堆在基地西北角 (70–77,109–111)**：5 座 GAPILL + 6:24 临时补的 **ATESLA@70,111**（全档案第 3 次见敌方自建 ATESLA）。这批塔不在我方从东北 (116,85) 的直线上，但 ATESLA 距 GACNST 只有 7 格，**仍然换掉我方 9930（全局损失 96.3%）**。
6. **单个骚扰单位会重置 `siegeQuietSeconds`**：5:24 全歼后我方已 28 单位/15920 对敌方 0，但 5:42 一只 ADOG 进入 field ⇒ 转攻从 5:34 推到 5:55（迟 21 秒）。

---

## 4. 战术库变更（`tactics.md`）

| 条目 | 变更 |
|---|---|
| **T-044（新增）** | 「判断它能否掏出第二种地面雪球：看 **GATECH 的建成时间 + 在出矿的矿车数**，不看'GATECH 是否存在'」。证据：match-015（GATECH 5:00 晚到 + CMIN 被杀一半 ⇒ 到被拆为止 0 辆 MGTK/SREF）支持 + match-014/012（GATECH 正常 + 矿车充足 ⇒ 必出重单位）反对 → **候选（支持 1 / 反对 1）**。量化了 T-038 的判据。 |
| **T-017（补证据，★升级为硬要求）** | 新增 match-015 段：`strongDef:2` **数量 + 位置（`side=0`）+ 排产优先级（`DEF_PRIORITY`）** 三个失效点一次性全部修好并全部生效；9 座防御建筑零损失、终局 `damaged` 为空。新增硬要求：(a) 初始 plan 就写 2；(b) 不要为给 `strongDef` 排队而抬 `baseDef`。置信度维持 支持 3 / 反对 0 → 确认，范围扩到盟军镜像局。 |
| **T-043（补正面样本，升为确认）** | 新增 match-015 段：三条件（`atEnemyHome` 斜率 / 是否在补静态防御 / 价值比 ≥2 倍）逐条核对后 4:19 与 4:54 两次否决出击，等到 5:56 才出门，63 秒结束。置信度 **支持 1 → 支持 2 / 反对 0 → 确认**。 |
| **T-027（补证据）** | `aaDef:2` 预置 + 不因"首波只有 2 架 JUMPJET"而下调：NAFLAK 0:42/0:52 落地，整局击落 11 架（6600），基建 2000 在第一波交火前就回本。支持累计 → 支持 6 / 反对 0。（注：本局 `build` 日志里 0:32/0:42 两次 `排产防御 NAFLAK（1000…）` 都是 `defenses.aaDef:2` 的预置排产，**不是** `airSeen` 反射——首见空军在 2:43。）
| **T-042（补证据，不改变结论）** | 第 5 个盟军复合速攻样本：第二段"没来"——它 5:00 有 GATECH 却 0 转化;本局 `aaDef`/反装甲两段账仍然分开核算（第一段 5:13–5:24 零损；攻城段 9930 全部由静态防御产生）。 |
| **T-041（补证据，未变口径）** | 判负集合（建造厂+兵营+战车工厂+空指部）第 3 次被验证：6:42–6:57 十五秒内 4 类全倒 → 6:59 判胜。同时暴露一个新的小缺口（写进 runtime_issues #1）：**GAWEAP 倒下的同一刻 `siegeTarget` 已复评切到 rank 2 的 GAPOWR**——本局无代价（`[over]` 同秒），但若 rank 1 提前拆完，队伍会去磨一座对判负零贡献的建筑。 |
| **T-024 / T-034 / T-026 / T-016 / T-029 / T-037 / T-039** | 本局**未触发或零证据**，一律不记"有效/无效"：`minerEscort:0` 无护卫也无矿车被点名偷袭；`defendRetreatRatio`/`defendMinValue`/`retreatRatio 0.35` 零次触发（防守交战全域我方占优、攻城期价值只掉到 65%）；`harass` 未使用；矿车数封顶 6 辆（`minersPerRefinery:2` 从未成为瓶颈）。 |

---

## 5. 下一代方案：**不升版本，v013 继续作为当前方案（diff 为空）**

任务书的规则是"本局赢了 → 只做针对性的小改进，最多改 2 个字段"，同时"**如果决定不改，不要 promote，在 debrief 里说明原因**"。**本局的结论是"没有字段值得改"**，逐字段理由：

| 候选字段 | 数据怎么说 | 决定 |
|---|---|---|
| `defenses.strongDef`（保持 2） | 两座 TESLA 都落地、都在轴线上、零建筑损失；这是 match-014 直接推导出的值，本局是它的正面验证 | **不改** |
| `defenses.baseDef`（保持 3） | 3 座 NALASR 从 1:02–1:13 一直活着，`baseDef` 全程被满足，从未把 `strongDef` 饿死 | **不改**（并写进 T-017 的硬要求） |
| `defenses.aaDef`（保持 2） | 11 架 JUMPJET 被击落（6600）；**首波只有 2 架 JUMPJET 也不该下调**（5:03 第二段一次甩出 6 架） | **不改** |
| `minerEscort`（保持 0） | 本局 0 次矿车被偷袭、0 次护卫拦截 ⇒ 无信息；match-009+014 已定案为负收益 | **不改** |
| `minersPerRefinery`（保持 2 / maxMiners 12） | 本局 6 辆矿车在 3:30 到终局都没成为瓶颈（中段资金 700–1400，进攻发起时 17720 的部队价值）；对手 CMIN 只有 6 辆。**唯一支持上调的理由是对面 11 辆矿车的经济压制**（match-014 型），本局不存在 | **不改**（v013 的理由里已写"只在确认盟军且长局时才提"） |
| `siegeAutoAttackUnits`（保持 14） | 本局 5:55 以 30 单位触发、时机刚好；调低会在 5:24 后带着 28 单位更早出门（对家还有 5 座 GAPILL + 后来那座 ATESLA），调高会白等 | **不改** |
| `attackMinUnits` / `retreatRatio` / `siegeQuietSeconds` / `leashRadius` / `attackTarget` / 经济字段 | 全部零次触发或表现正常 | **不改** |

**因此 v013 保持为当前方案（`evolve.py promote` 未执行，`plans/current.txt` 不变，无 v014）。** 本局产出的两条**改法都不是 plan 字段能表达的**，已写进 `runtime_issues.md` 交主会话：
- **#1 `siege()` 目标复评没有防抖**（同一 rank 的目标会互抢、行军中途换目标；本局 5:56 出发到 6:42 第一条建筑倒下之间有 46 秒无战果，其中 6:26–6:41 的 9 条 `[loss]` = 4440 发生在行军末端/接敌段）。建议 `runtime.js:682-761` 里"当前目标仍在候选里且 rank 不劣于新最优时继续打"，并"rank ≤1 的建筑还在时不把 rank 2+ 设为 `attack.target`"。
- **#2 攻城态下"建筑队列已空闲"warn 仍照发**（5:43 空闲 45 秒 / 6:43 空闲 105 秒），文案主动请求追加建筑，而 T-013 记载 ≥1500 的 buildOrder 项会在资金 0 时占死队列。建议加 `stance !== 'attack' && !M.attack && credits >= 1500` 前置条件。

**下一局的验证点（写进下一次 learner 的检查清单）**
1. T-044 的两条判据（GATECH 建成时间 + 在出矿矿车数）能否在实时阶段就提前给出"它这局造不出重单位"的结论——本局是**赛后**才确认的，实时角色当时只能看到"GATECH 5:00 才建"。
2. runtime_issues #1 修好后，攻城段的"行军/接敌空窗"是否缩短（本局 5:56→6:33 = 37 秒无战果）。
3. T-043 的第 3 个样本：若再遇到"长静默 + 它在补静态防御"，是否仍然能忍住不出击。

---

## 6. 执行层问题摘要（完整版见 `runtime_issues.md`）

- **`error` 类事件 0 条**；三条 match-014 修复（`side=0`、`DEF_PRIORITY`、以及 match-014 runtime_issues #7 记的 `C.state().buildings` 潜在崩溃）**本局全部确认已生效/已消失**，建议把 #7 标记为"随代码重写消失"。
- **#1 `siege()` 目标复评没有防抖**（次要，建议修）：5:57 选定 GACNST → 6:42 复评换成 GAAIRC，6:47 再换 GAWEAP；终局目标停在 rank 2 的 GAPOWR。本局代价约"白走 15 格 + 37 秒无战果"（直到 6:42 才有第一条 `[kill] 摧毁敌方建筑`），期间 6:26–6:41 的 9 条 `[loss]` = **4440** 发生在行军/接敌段。
- **#2 攻城态"建筑队列空闲"warn**（次要，建议修）：5:43/6:43 两条，本局代价 0，但文案会诱导在资金 0 时追加建筑。
- **#3 `C.intel()` 与决策之间约 6 秒采样滞后**（通信纪律，非缺陷）：分析员 6:00 因此发了一条错误的 urgent（"反射没触发"），6:18 自我更正；指挥官按"以页面日志为准"忽略，零损失。建议 `intel.status` 暴露决策序号，并写进 analyst 纪律。
- **#4 任务书模板的 dump 分页指引有两处缺口**（工具层/流程，非 runtime）：(a) 模板的 `snapshots stride:2` 会把 5:13–5:24 那场决定性塔线歼灭战压缩成"5:01 与 5:21 各 8 单位/4680"的直线——**改用 `stride:1` 拿到全量 43 张**才能看到敌方 army 从 7430 掉到 200；(b) "offset 每次加 300 直到空数组"的循环对 `log` 是错的（过滤后只有 182 条，`offset:182` 返回空数组会诱导提前收工），**正确判据是 seq 覆盖度**（182 + 排除的 42 = 224 = `meta.logCount`，`min(seq)=1`/`max(seq)=224`）。**归档本身没有丢数据**（我在复盘时一度怀疑桥输出被截断，但两次重跑都得到同样的 182 条、逐条 diff 182/182 全等、未能复现，因此不写成"桥会截断"这个未证实的结论）。建议把模板改成 `log` 用 `limit:50` 分页、`snapshots` 用 `stride:1`，并加上述两条断言。**这条与 `RA2_BRIDGE_DIR` 一起列为给主会话的改动项。**

---

## 7. 环境适配说明（按要求如实记一笔）

**本局的实时角色任务书被主会话改过，原因是环境适配，不是方案变化。** 任务书模板原文写死了 `mcp__Claude_Browser__javascript_tool`，而本机**没有这个工具**；改法是使用等价的只读 CDP 桥：

```
export PATH="/opt/homebrew/bin:$PATH"
node /Users/lukeliu/ds/_ra2-bridge/cdp.js evalfile /tmp/ra2-cmd-learner.js --target gonghui
```

- 片段可以 `await`，**必须以 `return <值>` 结尾**（桥包进 async IIFE，返回值原样打印 stdout）。
- 对应改动：`matches/match-015/agents/*.md` 的「工具边界」节（说明桥是本环境里 `mcp__Claude_Browser__javascript_tool` 的等价替代、禁止点击/刷新/导航），以及 `scripts/evolve.py` 新增的 `RA2_BRIDGE_DIR`（默认 `/Users/lukeliu/ds/_ra2-bridge`）。
- **本局全程遵守只读约束**：没有刷新、没有导航、没有点击；`window.__cmd` 在赛后仍在（`over === 'won'`），因此归档走的是页面内存 dump 而不是 localStorage 兜底（`ra2cmd:lastMeta`/`lastLog`/`lastSnapshots` 三份兜底也都确认存在，未使用）。
- 这条改动**不涉及任何用于对战的 plan 字段或 runtime 行为**，只影响"角色怎么读页面"。

---

## 8. 主会话审核后的落地改动（复盘学习员交稿之后补记）

学习员的报告由主会话逐条审核，按"证据是否成立、修法是否有把握"分流，结果如下。**runtime 版本因此从 `rt-5e310d3147` 变为 `rt-30428d6bd1`，下一局 `new-match` 会自动触发重装，本局归档仍对应 `rt-5e310d3147`。**

| 问题 | 处理 | 落点 |
|------|------|------|
| #1 `siege()` 每 0.7 秒无条件复评目标 | **已修** | `scripts/runtime.js` 的 `siege()` |
| #2 攻城态仍发"建筑队列已空闲"warn | **已修** | `scripts/runtime.js` 的 `production()` |
| #3 intel 读数滞后导致误报 urgent | **不改进 runtime，改角色纪律** | `agents/analyst.md`、`agents/commander.md` |
| #4 任务书 dump 分页指引有缺口 | **已修** | `agents/learner.md` |

**#1 的修法（与建议略不同）**：没有采用"给 rank 加 -0.5 偏移"，因为那会在同级建筑之间持续抖动而没有确定解；改成**以 rank 为界的粘性**——当前目标仍在候选里、且它的 rank 不高于本轮最优候选的 rank 时，继续打它；只有出现严格更优的等级、或当前目标已被拆掉时才换。这样既消掉"同 rank 两端来回穿越"，又保证 rank 0 的建造厂还在时不会被 rank 2 的电厂顶掉。**学习员建议的第 3 点（rank ≤1 还在时不要把 rank 2+ 设为 `M.attack.target`）不需要额外代码**：候选取的就是最小 rank，只有在判负集合全清之后才会落到 rank 2，而那已经是终局。

**#2 的修法**：该 warn 的条件加上 `stance !== 'attack' && !M.attack && credits >= 1500`，并把这三个便宜判断放在 `every('sqIdle', 60)` **之前**，避免条件不成立时白白消耗限流器。文案指向的真正缺口（`buildOrder` 没有兜底项）留给学习员改方案，不在攻城期发号施令。

**#3 的取舍**：学习员建议在 `intel.status` 里暴露决策序号。主会话没有加这个字段——现有 `status` 已经很长，而且这个字段只是把"该看日志"这件事再包一层。改成在任务书里写死纪律：**判断"某个反射有没有触发"必须以 `C.log` 里最近的 `[plan/main]`/`[attack]`/`[reflex]` 事件为准，不要只看 `status.stance` 字面量；同类指控要连续两次采样（间隔 ≥15 秒）都成立才发**。成本为零，且直接对应本局那次误报。

**#4 的修法**：`log` 分页改 `limit:50`，`snapshots` 改 `stride:1`（`limit:15`），并写入两条强制断言（`log` 用 `seq` 覆盖度而不是"读到空数组"判完；`snapshots` 断言长度等于 `meta.snapshotCount`）。

**验证**：`node --check scripts/runtime.js` 通过；`evolve.py status` 确认 `currentPlan` 仍为 `v013`（学习员判定不升版本，主会话同意：8 个字段逐条有据不改，本局赢得干净且没有可归因于字段的缺口）。

**未改**：学习员提到的 match-014 `runtime_issues` #7（`C.state().buildings` 潜在崩溃）按其自查已随代码重写消失，未再动。
