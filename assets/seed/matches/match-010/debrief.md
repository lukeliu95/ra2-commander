# 复盘 match-010

## 结果
- **负**，用时 **6:56**（416 秒）。方案版本 **v009**（`rt-f897c162d5`），本局是 v009 首战。
- v009 历史战绩：0 胜 1 负（仅本局）。父版本 v008 未曾实战使用（0-0）；v009 未满足"累计 ≥2 败"的回退条件，无需 `evolve.py use` 回退到父版本。
- 我方 NA（苏军）@71,118；敌方盟军 @135,80——**本系列第一次"我方苏军 vs 敌方盟军"的组合**，出生点组合与 match-001/002/006 相同（只是双方阵营互换）。

## 胜负关键（3 条，带数据）
1. **对手打出了一种全新的"复合速攻"打法：JUMPJET 速攻变体被我方成功防住，但 90 秒后紧接着换成完全不同的 MGTK/SREF 碾压变体**。3:02 首波 6 架 JUMPJET+FV+MTNK（field 4950→7050，超出档案首波区间上限），我方以 TESLA（2:26 提前落地）+3 NALASR+NAFLAK+及时的 `aaDef 1→2` 调整，在 4:07 打出 2.08:1 交换比、歼灭 20 架 JUMPJET，敌方一度撤退。但仅 90 秒静默期后，5:48 敌方甩出 match-006"经济碾压变体"的招牌单位 MGTK(3)+SREF(2)，此时我方军力才恢复到 9-10 单位/2430-2930（远不足以应对 5400-11250 价值的地面波），6:03 起防线被击穿，6:37 建造厂失守，6:56 判负。**这是全档案第一次证明两种此前认为互斥的盟军变体会先后叠加出现，而不是二选一**。
2. **执行层 bug：矿车被"防线外撤回"反射派回基地后，永远不会被重新派去挖矿**，导致 `HARV` 数量从 3:22 起冻结在 5 辆长达 3 分钟（`targetRefineries:4` 从未达成），资金几乎全程为 0，直接削弱了两波之间本该有的经济/军力恢复速度。主会话在 t≈5:10 现场发现并手动修正，回溯发现这条反射在 match-005/007/008/009 里也至少触发过 7 次，此前从未被现场核实过——是一个至少存在 5 局的长期未察觉执行层缺口（详见 `runtime_issues.md #1`）。
3. **终局交换比与整局交换比严重脱节，说明"打赢野战"不等于"守住基地"**：首波单独口径（3:02-4:07）我方交换比 2.08-2.11:1 明显占优，但整局 `tally` 最终是 20380/28140≈0.72:1（净亏）——真正拉低比例的是 6:03 之后我方失去建筑生产能力时期的单向失血（大量建筑被白拆）。

## 对手新行为/变化
- **首次识别"复合速攻变体"**（match-010 首次，1 局样本）：JUMPJET 速攻（match-007 首次识别的变体）被击溃后，静默期可能短到 90 秒（远短于 match-006 碾压变体的 4-6 分钟），紧接着换成完全不同的 MGTK/SREF 地面碾压波。已写入 `opponents/ai-easy.md` 盟军小节新增的独立子节，并更新"四种盟军雪球打法"对比表。
- JUMPJET 的全视野建成时间刷新到 1:41（此前最早是 match-006 的未精确记录/match-007 的 3:26 实时观察量级），实时侦得规模化混入首波的时间 3:02 也比 match-007 的 3:50 早 48 秒。
- 验证了"地面部队完好时确实能打下驻扎的 JUMPJET 编队"（match-007 因为地面部队提前归零没能测试，match-010 首次正面验证成功，20 架 JUMPJET 被歼灭）。
- 本系列第一次"我方苏军 vs 敌方盟军"组合，进一步坐实"出生点、我方阵营、敌方阵营是三个独立随机变量"的结论。

## 战术库变更
- **新增候选 T-033**：打赢盟军"JUMPJET 速攻"雪球后不代表安全，需要立刻按"可能有第二种完全不同的雪球（如地面碾压）"准备，静默期可能短到 90 秒。1 局样本，候选。
- **T-017（反装甲塔预置）**：追加 match-010 证据——单座 TESLA 在复合速攻的第一波里就阵亡（3:26），此后到判负都没有反装甲塔在场，暴露"1 座塔挡不住需连续扛两波"的场景；但本次复盘评估后**未采纳把 `strongDef` 提到 2**（成本效益证据不足，详见字段 diff 部分的理由）。
- **T-027（aaDef 预置）**：升级为支持 3（新增强支持样本）——`aaDef` 从 1 提到 2 后对 JUMPJET 首波拦截效果显著（2.08:1，本系列最佳），但同时明确了这条规则的边界：只解决空中威胁，对随后的地面碾压波没有任何贡献，不能把"防空调整生效"误读为"防御已经足够"。
- **T-026（矿车护卫）**：追加说明——本局发现的矿车永久闲置 bug 是"撤退反射缺少复工逻辑"，与 T-026 提案（护卫站位/主动迎击）是两个不同的问题，护卫实现即使修好也不能替代"撤退后能复工"这个更基础的缺口。
- **T-029（defendRetreatRatio/defendMinValue）**：追加 match-010 反例——`defendMinValue`（绝对值 800）在开局阶段（部队总值 720，无战损、正在打赢）三次误触发"撤退"，暴露该字段缺少"确实发生过战损"的前提条件，已记为已知局限（代码层修复建议见 runtime_issues.md #2）。

## 下一代方案：本次复盘**不 promote**，当前方案继续保持 v009
- **理由**：本局唯一直接可归因的败因分两类，都不适合用 plan 字段解决——(1) 矿车永久闲置是纯执行层 bug，需要代码修复（见 runtime_issues.md #1），不是 plan 能表达的；(2) "复合速攻"是本局才首次发现的新对手变体，只有 1 局样本，且我方对其第一阶段（JUMPJET 速攻）的应对（`aaDef` 预置+及时调整）本身已经验证非常有效（2.08:1），没有找到证据支持"改哪个 plan 字段能让第二阶段（MGTK/SREF 碾压）的应对更快"——曾评估过把 `defenses.strongDef` 从 1 提到 2（增加反装甲塔冗余），但盟军对手不像古巴那样有一击必杀的拆塔手段，且 match-009 已有"1 座反装甲塔对陆战型对手足够"的正面证据，现在只凭 1 局"塔在首波阵亡"的样本就加码证据强度不够，故未采纳。
- **待下一局验证后再决定**：(a) 矿车 bug 修复后，同类对局的经济恢复速度会明显加快，届时更容易判断"复合速攻"的第二阶段是否还需要额外的 plan 调整；(b) 如果再遇到一次"反装甲塔在首波中途阵亡、且静默期短于 2 分钟"的样本，应把 `strongDef: 2` 从"评估未采纳"升级为候选证据。
- 当前 `plans/current.txt` 仍为 `v009`，未创建新版本文件。

## 执行层问题摘要（完整见 `matches/match-010/runtime_issues.md`）
1. **【关键】矿车永久闲置 bug**：`army()` 里矿车被撤回基地后没有任何复工逻辑，指令队列清空即永久闲置。本局 3 次触发（3:43/3:57/4:40），主会话已现场手动修正一次；回溯发现 match-005/007/008/009 至少还触发过 7 次，此前均未被察觉。建议在 `M.retreatedMiners` 里跟踪被撤回的矿车，威胁解除+血量恢复后重新派回最近矿格（复用 T-026 修复里的 `oreNear()`）。**需要主会话下一轮代码修复，优先级最高**。
2. **【次要，已知局限】`defendMinValue`（800）在开局阶段部队总值天然偏小时会误触发撤退**：1:18/1:34/1:43 三次在"零战损、正在打赢"的情况下触发。建议加一个"本场交战确实发生过战损"的前提条件（如 `defendRatio < 1` 或已有 ≥1 次 loss 事件）。
3. 其余（siege/leash/tally/核电站相关逻辑）本局均按预期工作，无新异常。

## 主会话补记：执行层修复（复盘后，进入 match-011 前）

针对 `runtime_issues.md` 的两条问题，`scripts/runtime.js` 做了以下改动（`node --check` 通过）：

- **#1（关键）矿车复工**：新增 `M.retreatedMiners`（Map<id, {t}>），`exposedMiners` 触发撤退时把矿车 id 和时间戳记进去。`army()` 每 tick 独立检查这张表（放在函数顶部、不受 threats/attack/harass 分支影响，保证战斗激烈时也照常生效）：矿车血量恢复到 ≥90% 且附近 25 格内没有敌方单位时，重新派回离它当前位置最近的一个矿格；如果等了 40 秒还不安全，也强制派回去（继续挖矿至少比原地闲置好）。这条 bug 经学习员回查确认在 match-005/007/008/009 至少还发生过 7 次、全部没被抓到，是本系列迄今影响面最广的一个静默经济漏失。
- **#2（次要）`defendMinValue` 误报**：加了 `defendRatio < 1` 的前提条件——只有这场交战确实掉过血，绝对值下限才会触发。之前部队总值天然偏小的开局阶段（比如 8 个动员兵才 720，低于默认 800）即使正在打赢也会被误判成"撤退"。

两处都无法用 plan 字段表达，是纯代码修复。文档同步：`references/plan-schema.md`（矿车复工行为、`defendMinValue` 的新前提条件）。

方案层本局维持 v009 不变（学习员判断：两个直接败因都是执行层问题/单样本对手变体，均衡证据不足以支撑 plan 字段调整）。
