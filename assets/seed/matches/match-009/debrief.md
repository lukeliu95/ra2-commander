# 复盘 debrief match-009

## 0. 归档与数据来源（先说这一条，因为它影响所有数字的可信度）

本局结束时**页面已经被关闭**：`window.__cmd` 已不存在，CDP `http://127.0.0.1:9222/json/list` 返回 **0 个 page target**（浏览器还在，target 列表为空），因此任务书第 1 步的三次 `dump()` 都无法执行。

**但数据没有丢**——rt-cea4cb56a8 的 localStorage 兜底（在判定 `over` 的那一刻写入的三份 dump）仍然躺在 Chrome profile 里。复盘时从 `_ra2-bridge/chrome-profile/Default/Local Storage/leveldb/000004.log` 里按 LevelDB WriteBatch 格式解出了三条记录：

| localStorage 键 | 恢复结果 | 与 meta 自述的一致性 |
|---|---|---|
| `ra2cmd:lastMeta` | 1637 字符 JSON，15 个键 | `result=won`、`durationSec=466`、`planVersion=v007` |
| `ra2cmd:lastLog` | **229 条**（UTF-16LE） | `meta.logCount = 229` ✅ |
| `ra2cmd:lastSnapshots` | **48 张** | `meta.snapshotCount = 48` ✅ |

三份文件已写入 `meta.json` / `log.json` / `snapshots.json`，`timeline.md` 由 `summarize_match.py` 正常生成（236 行）。**本局是 match-006 那次"现场数据全丢"之后新增的 localStorage 兜底第一次真正被使用，验证有效。** 归档数据完整，本 debrief 的所有交换比/时间点都是从 `log.json` 逐条重算出来的，不是照抄实时日志。

**版本链的一个意外**：`evolve.py status` 显示本局开打时用的 v007 **不是**当时的最新版——match-008 的复盘在 **12:07:47**（本局刚结束）promote 了 v008（父版本 v007，唯一改动 `defenses.baseDef 2→3`）。所以本局是 v007 的最后一战，下一代 v009 的父版本是 v008，**v008 的 baseDef:3 从未上过场**。

## 1. 结果

- **胜**，游戏内 **466 秒（7:45）**，方案 **v007**，runtime `rt-cea4cb56a8`，对手 `ai-easy`（苏军 / Russians），地图"岛屿之战 (2)"，出生点我方 (135,80) 东北、敌方 (71,118) 西南。
- **v007 的历史战绩：1 胜 1 负**（match-008 负 4:20 → match-009 胜 7:46）。任务书里"v007 首战即胜"的说法**不准确**：本局是 **v007 的首胜**，它的首战（match-008）是败仗。
- **本系列 9 局里的第 2 胜**（上一次是 match-002 / v001，447 秒），也是：
  - **第一次以苏军身份取胜**（我方 Arabs 是 NA 系苏军：NACNST/NAPOWR/TESLA/HTNK；上一次当苏军是 match-004，负）；
  - **第 3 次对阵苏军 AI 的第一次取胜**（苏军对手：match-003 负、match-004 负、match-009 **胜**）；
  - **第一次在 match-001/002/006 那套出生点组合下取胜**。
- 用时 466 秒在本系列 2 场胜利里是最长的一次，但**结局形态最好**：不是"守到它被打崩"，是"拆掉它的建造厂"。敌方终局只剩 NAREFN×3 + NADEPT + NATECH，我方终局仍有 15 辆犀牛。

## 2. 胜负关键（3 条，都带重算数据）

1. **TESLA（`defenses.strongDef: 1`）在 2:26 落地，全场防御建筑零损失。** 它是 v004 起就写在初始 plan 里的（**不是 v007 的新增项**——v007 相对 v006 只改了 `aaDef 0→1`、`siegeAutoAttackUnits 8→14`、`aaVehicle` 补回 1），但本局是它第一次在苏军局真正及时落地：2:00 以"资金 2161"正常排产、**2:26 落地 @132,86**（比 T-017 的 3:30 底线早 64 秒），比 match-004（整局零反装甲建筑）和 match-008（ATESLA 到判负都没上场）都实质改善。塔线（TESLA + 2 NALASR + 1 NAFLAK）吃掉敌方 2:45–2:51 的 **27 单位/5790** 大波和 5:29–5:35 的 **16 单位/5400** 第二波，**快照里我方 4 座防御建筑从落地到 7:46 一个不少**；敌方部队价值 5790（2:52）→ 990（3:42）。
   **⚠ 这里要纠正两份实时日志的核心数字**：两位角色都写"2:56–3:41 塔旁交换比约 **2.8:1**"，逐条重算是 **1.76:1**（杀 3 HTNK + 2 HTK + 1 DOG + 10 E2 = 4800；损 **2** HTNK + 8 E2 + 1 DOG = 2720）。他们把两个 DOG 数字对调、我方 HTNK 损失少数 1 辆、敌方 E2 击杀多数 4 个。**而且更根本的是口径错误**：档案的 1.13:1 是 match-004 的**整局**口径，1.76:1 是 match-009 的**单场**口径，两者不可比；按同口径重算 match-004 的分窗口值是 1.39 / 1.17 / 2.42，**1.76 落在这个区间中间**。所以 **"TESLA 把交换比从 1.13 提到 2.8"这个说法不成立**，T-017 的价值由"基地不被拆 + 把敌方科技节奏推后 1–2 分钟"支撑，不由交换比支撑。
2. **经济第一次没被拖垮，而且反过来掐死了它的经济。** 我方 4 矿厂 8 矿车、**全局 0 矿车损失**（match-004 同期资金从 1:20 起恒为 0），同时**击杀它 9 辆 HARV**——它的矿车峰值只有 **7 辆**（档案 12–13 辆），资金从 2:02 起长期为 **0**（快照 2:02/3:32/4:02/4:32/5:02 全是 0）。**这是本局最大的新发现：它中后期"资金富余、没有反攻窗口"是建立在经济没被打的前提上的；打矿车能直接把窗口打开。** 本局 5:19 那个 3.56 倍兵力差，一半来自我方攒兵，另一半来自它养不起兵。
3. **5:19 转攻（5:41 出发）是全局面貌的转折，但这个决定是人做的。** 5:19 我方 **25 单位/16020**（17 HTNK + 8 E2）对敌方**全部 15 单位/4500**（`field` 连续 90 秒为 0、`atEnemyHome` 15 个），3.56 倍是本系列最大兵力差；同时它第 1 座哨戒炮到 4:53 才出现（进攻路上没有塔阵）、T-019 的"转科技"信号已现（V3 4:17）而天启还没出来。指挥官 `apply({stance:'attack'})` → 5:41 发起 27 单位攻势 → 6:04 敌方全场归零 → **6:34 拆掉建造厂** → 7:45 胜。
   **时间线要点（核实过，实时日志的说法要打个折）**：页面时间轴上**敌方 5:29–5:35 先全军出动**，我方 5:40 才转攻、5:41 出发——**是敌先动、我后动**，不是"我们出击把它引出来"。（指挥官日志里"5:19 转攻"是它自己的时钟，与页面差约 21 秒，引用一律以 `log.json` 的 5:40/5:41 为准。）这不削弱结论，反而让 T-031 更干净：**规则描述的是"观察到它 100% 投入、正压向我方塔线"这个可观测条件。**

## 3. 对手的新行为/变化（苏军档案需要修订的部分）

1. **"1:06 出门 20 E2"这条秒级复现的脚本本局整波缺席。** 1:07 家 14 E2 → 1:42 家 16 E2 → 2:19 家 14 E2 + 2 HTK，`field` 一直只有 1–2 个单兵摸 (120–126,84)（5 次试探全被零损失秒杀）；首波**最少迟到 99 秒并被合并**进 2:45 的大波（家峰值 **27 单位/5790**，比档案最大的第 2 波还大 5%）。**已把"1:06 出门 20 E2"从硬节点降级为"最早可能时间"**，并把"主攻出门时间随我方防御强度推迟"从盟军 1 次观察升级为 2/9 局。
2. **静态防御与科技整体右移 1–2 分钟，但经济节点准时。** 第 1 座 NALASR **4:53**（档案 ≤1:00）、3 座到 5:13/5:34；核电站 **5:35**（档案 2:41–3:21）；第 3 矿厂 4:14；维修厂 6:08、实验室 5:22、自建 TESLA 6:25（后三者在**我方打进它家门之后**才补）。而第 2 矿厂 **2:04**（档案 2:01）、首辆 HTNK **2:23**（档案 2:21）完全准时。**结论：被打崩的是它的建筑/科技节奏，不是它的经济底子。** 需要区分"它就是这么打的（先军后工）"与"2:56–3:41 惨败把节奏推后了"——两者在本局数据里同时存在。
3. **天启 APOC 整局未出现**（档案窗口 4:46–6:21，match-004 实测 4:46）；**V3 只有 1 辆且随波进攻**（4:17 首见，晚 16 秒，5:55 被击杀后没再补），**从未在射程外轰到任何一座我方塔**——这是档案里"V3 定点轰塔、守方零反制"（2/3 局）的第一次反例。**V3 有"单独狙击"和"随波被吃"两种形态，后者出现在它被压制、造不出第二辆的时候。**
4. **首次观察到它 100% 投入**：5:29–5:35 把 16 单位/5400 全部押上，家里 `atEnemyHome` 变 **0**（match-003/004 都是"主力出门 + 家里留 10+ 预备"）。这是最好的反攻时刻（T-031）。
5. **挨打后"缩回家里静止"是 3/3 局的默认反应**：3:41 被打崩后残余 11 单位全缩在家、`field` 连续 90 秒为 0，直到 5:29 才再次整军出动。
6. 出生点新样本：苏军 AI 首次出现在**西南 (71,118)**（前两次都在东北 (135,80)），进攻轴随之镜像到 (120–126,84)。**出生点是独立随机变量这条，苏军局也齐了。**

## 4. 战术库变更（`tactics.md`）

**新增 2 条**
- **T-031 最佳转攻时刻**：敌方 100% 投入（`atEnemyHome`≈0）且正朝我方塔线行军时转 `attack`——让这波先撞塔线消化掉，撞完它家是空的。证据链：4:56 全在家 → 5:29–5:35 全军出动 → 5:40 转攻 / 5:41 出发 → 6:04 敌方归零 → 6:34 拆建造厂 → 7:45 胜。置信度 **候选（1 局）**，并注明本局"等它撞塔"与"先手出门"两种做法没有完全分离。
- **T-032 拆核电站会引发核爆，必须最后拆并先散开**：7:25 `[kill] 摧毁 NANRCT` 与 `[loss] {"HTNK":3}` 同一时间戳，一次 2700，是本局单次最大战损；而 `siegeRank()` 恰好把 `rules.power>0` 排 rank 1（核电被**优先**打），等于主动往炸弹上凑。置信度 **候选（机制确认、收益未验证）**。

**升级/修订 8 条**
- **T-017**（升级为"最高价值的防御条目"，支持 3/0）——补上 match-009 三条硬证据；**同时纠正两处归因错误**（strongDef 自 v004 就在初始 plan 里，不是 v007 的功劳；交换比数字与口径见上）；追加意见：**反对把 `strongDef` 加到 2**（1 座塔对陆战型苏军已经够用，那 1500 该给犀牛）。
- **T-019**（候选 → **确认，仅限苏军**）——应对方向整体反转：从"把静默期换成兵力、准备防守"改成"**识别到它转科技就立刻主动出击**"。前半句"全歼波次→它转科技"2/3 局成立；后半句"会换来更致命的一波"只在"继续守着不动"时成立（match-004），主动出击能掐死它（match-009）。
- **T-005**（候选 → **确认**）——match-009 的 3.56 倍出兵是本系列最大兵力优势，且直接导向胜利；同时记录"达到门槛但没人按按钮"的执行层风险。
- **T-024**（支持 0 → **支持 1**，首个正向支持）——修订为"`field=0` 持续 ≥30 秒时：达标就 `attack`，不够才 `harass`"（本局派小规模骚扰会被它家里 15 个单位吃掉）。
- **T-023**（候选，新增第 3 种失效模式）——本局触发 3 次（1:07 / 1:26 / 2:42）**全是苏军"在家攒一坨"的假阳性**，`urgent` 的 2 倍门槛一次未触发。**新增校准：已知对手是苏军时，`atEnemyHome` 反超要降权解读**，改看"它的建筑/科技节奏是否也提前"和"矿车数是否也在涨"。
- **T-004**（AA 部分 3 → **4 次反对**）——并写下关键区分：**`aaDef` 必须预置（T-027），`aaVehicle` 不必**（`airSeen` 对权重即时生效）。
- **T-022**（适用条件收窄）——机制结论不变，但"温水煮青蛙"只在"我方兵力不占优 **且** 无塔火支援 **且** 被动原地接战"三者同时成立时才应验（match-007 三者全中，match-009 三者全不中）。
- **T-028 / T-029 / T-011 / T-021 / T-026**（补记 match-009）——T-028 记录"方向反转：该触发时完全不触发"；T-029 记录"本局未触发、仍为未验证"；T-011 支持 3→4、T-021 候选→确认（6 次拴绳 warn 全部正确、迎击点全在塔覆盖区）；T-026 记录实现偏离提案。

**废弃：无新增**（保留全部历史条目）。

## 5. 下一代方案 v009

```
promoted: v009   parent: v008
diff: vehicleMix: {tank:4, aaVehicle:1} -> {tank:4}
```

**只改 1 个字段**（本局赢，按"最多 2 个字段"取证据最硬的；第 2 个候选都没有足够证据，见下）。删 `aaVehicle` 而**保留键不写 0**（写 0 会按 plan-schema 显式关闭 `airSeen` 反射）。证据三层：(1) 本局指挥官 1:21 删掉它之后**全程 0 辆 HTK**、19 辆犀牛的峰值全给坦克，5:19 的 3.56 倍兵力差由此而来（若保留，按权重语义约 20% 载具产能变成 500 块的 HTK，5:19 大约只剩 13 辆犀牛）；(2) T-004 的 AA 部分早已废弃（现 支持 1 / 反对 4），已观测 7 局里 5 局敌方零空军（苏军 3/3、古巴 2/2）；(3) `runtime.js:177` 的 `airSeen` 反射对**权重**即时生效，所以这个对冲是冗余的——而 `aaDef` 不冗余（NASAM 1000 过不了 `cost<=700` 的资金饥饿快速通道，T-027），**故 `defenses.aaDef:1` 保留不动**。
**残余风险（已在 T-004/对手档案标注）**：若下一局是盟军镜像，0:00 到看到飞机之间没有防空车（match-007 是 3:50 首见 JUMPJET），届时靠反射补齐。

**明确不改的部分及理由**
- `defenses` 维持 v008 的 `{baseDef:3, aaDef:1, strongDef:1}`：本局 1 座 TESLA + 2 NALASR + 1 NAFLAK 在 27 单位大波前零建筑损失 → 反对把 strongDef 加到 2；v008 的 `baseDef 2→3` 来自 match-008 古巴 C4，本局（苏军、无 C4）不构成反驳，**保留但标注"从未上过场、仍未验证"**。
- `siegeAutoAttackUnits` / `siegeQuietSeconds`：本局一次都没触发，根因是**判据缺陷**（不是数值），改数值无意义 → 交主会话改代码。
- `minerEscort` 保持 1：本局零可观测效果 + 实现与 T-026 提案不一致（护卫站在**矿厂建筑**坐标而非矿区），实现修正前该字段无信息，**不删也不当成有效**。
- `targetRefineries 4` / `minersPerRefinery 2` / `maxMiners 12` / `maxFactories 2` / `infantryCap 8` / `leashRadius 4` / `attackMinUnits 10` / `retreatRatio 0.35` / `defendRetreatRatio 0.4`：本局这组配置实测健康（4 矿厂 8 矿车、0 矿车损失、4:19 第 2 工厂落地、6 次拴绳 warn 全部正确）。
- **本局新发现的两个最大战损点都没有对应 plan 字段**，只能靠改代码：核电站核爆（7:25 一次 3 辆 HTNK / 2700）和收尾拆家期的 11 辆犀牛（9900，是防守阶段全部战损 2720 的 3.6 倍）。

## 6. 执行层问题摘要（详见 `runtime_issues.md`，8 条）

> **复盘进行中查到：主会话已把 `scripts/runtime.js` 改到 `rt-f897c162d5`，下面的 1/2/3 的核心部分已经修好（代码注释直接引用了 match-009 的证据）**——1 的 `field` 判据 + T-005 优势门槛 + "反射被挡住"告警、2 的核电站排到最后、3 的护卫待命点改到矿格。**4/5/6/7 尚未修。** 所以下一局（match-010）实际是在"新 runtime + v009 新 plan"上跑，**本局的"手动转攻"这条经验届时可能不再需要人工介入**——这正是要用 match-010 去验证的。详见 `runtime_issues.md` 顶部的并发更新说明。

1. **【关键】`siege()` 的自动转攻反射结构性地永不触发**——安静判据是"全地图看不到任何敌方非建筑单位"（`runtime.js:534`），而本平台**无战争迷雾**、侦察过的区域永久可见，所以敌方缩在家里的 15 个单位永远算在 `S.hostile` 里。本局 4:32–5:38 其他条件全部满足（我方 15→25 单位含载具、`attackMinUnits`/`siegeAutoAttackUnits` 双达标、**期间零 alarm**），反射一次没动，转攻靠指挥官手动。**建议改用 `field` 语义**（`enemyStart()` 距离 >15 格，与 `intel()` 里 `enemy.visibleArmy.field` 同一算法）。
2. **【关键】拆核电站引发核爆，代码里没有任何"接近核电站时散开"的处理**，且 `siegeRank()` 把核电排在 rank 1（优先打）。建议核电排到最后 + 选目标前先散开离它 6 格内的单位。
3. **`minerEscort` 实现与 T-026 提案不一致**：护卫被 AttackMove 到**矿厂建筑**坐标（`runtime.js:513-524`），而提案要求待在矿区（`M.oreTiles`）。本局 0 矿车损失但也零可观测效果。
4. **收尾阶段"暂停拆建筑先打部队"会把坦克牵进残余塔的火力圈**：敌方 `field` 归零后我方仍损失 11 辆犀牛（9900）。建议把 `isBaseDefense` 提到 `power` 之前。
5. **`intel()` 没有战损/击杀累计口径**：两个实时角色手工累加 45 秒窗口，**独立地把 1.76:1 算成 2.8:1**。建议在 `C.intel()` 里加 `tally`（全量累计，与 `record()` 同口径）。
6. `intel().me.army.at` 是全体质心（含留守/落后单位），攻城期间与实际战况脱节（6:04 报 (111,94)，实际已在 (71,118) 拆基地）。
7. `buildOrder` 跑完（4:20）后无兜底项，队列空转 45/105/166 秒三 warn（资金基本为 0，实际浪费有限）。
8. **三项机制本局"未验证"，不要写成"已验证有效"**：`defendRetreatRatio`（本局**从未触发**，因为我方每次防守都打赢，价值始终在起始值 85% 以上；match-008 的缺口与新增的 `defendMinValue` 都没被检验）、**防空绕资金门槛 + 防空建筑本身（连续第 3 局未触发**，`airSeen` 全程 false）、`strongDef` 资金门槛绕开（用不上，TESLA 2:00 正常付清）。

## 7. 下一局（match-010）的观察清单

1. v009 删掉 `aaVehicle` 后，坦克产量/兵力峰值是否比 match-009 的 19 辆更高；若对手是盟军，`airSeen` 反射是否在 3:50 前后正常把权重提回 2。
2. 主会话若修好 `siege()` 判据：4:30 左右的自动转攻是否发生、是否比本局的手动 5:40 更早（本局本可早约 1 分钟）。
3. 主会话若修好核电站处理：拆 NANRCT 时是否还有"同一时间戳多辆 HTNK 损失"。
4. `minerEscort` 实现修正后，它在"对手偷袭矿区"的局里是否真的起作用（本局无法验证）。
5. `defendRetreatRatio` / `defendMinValue`：需要一场**真的打输的防守交战**才能验证，不要在没触发的局里给它记功。
6. 苏军档案修订是否成立：首波是否仍会缺席（"整军合并"变体）、静态防御是否仍右移、天启是否仍可能不出现。
